const defaultCrop = {
  x: 0,
  y: 0,
  width: 0,
  height: 0,
};

const xHandlersPosition = ["e", "w"]; // east, west
const yHandlersPosition = ["n", "s"]; // north, south
const xyHandlersPosition = ["nw", "ne", "se", "sw"]; // northWest, northEast, southEast, southWest

export {
  defaultCrop,
  xHandlersPosition,
  yHandlersPosition,
  xyHandlersPosition,
};
